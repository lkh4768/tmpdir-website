import React from 'react';
import { Container } from 'reactstrap';

class Body extends React.Component {
  render() {
    const node = (
      <Container fluid />
    );
    return node;
  }
}

export default Body;
